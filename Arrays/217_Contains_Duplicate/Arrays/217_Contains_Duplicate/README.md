# 217. Contains Duplicate

## Difficulty
Easy

## Pattern
HashSet

## Approach
We use a HashSet to store elements while traversing the array.
If an element already exists, return true.
Otherwise add the element to the set.

## Time Complexity
O(n)

## Space Complexity
O(n)
